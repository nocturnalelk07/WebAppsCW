<!doctype html>
<html lang="eng">
    <head>
        <title>@yield("title")</title>
    </head>
    <body>
        <h1>@yield("title") Page</h1>

        <div>
            @yield("content")
        </div>
    </body>
</html>